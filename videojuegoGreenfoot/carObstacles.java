import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class carObstacles here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class carObstacles extends Actor
{
    /**
     * Act - do whatever the carObstacles wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        //La carObstacles y todas las que la heredan son extremadamente simples, pues no poseen métodos internos
        //Solo necesitan una posición y pertenecer a la clase carObstacles para que el actor car pueda reaccionar adecuadamente
        //La única diferencia entre las clases carObstacles es su imagen
    }
}
